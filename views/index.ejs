<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>D&D 5th Ed. Monsters</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" type="text/css" href="styles/main.css" />
  </head>
  <body>
    <div class="container">

      <span><a href="https://www.dnd5eapi.co/">Dungeons & Dragons 5th Ed. Monsters</a>: <span class="badge rounded-pill text-bg-success"><%= count %> monsters found!</span></span>

      <div class="row my-1">
        <div class="box col-9 shadow bg-white rounded">
          <form id="myForm" action="/monster" method="post">
            <% monsters.forEach(monster => { %>
            <button type="submit" class="btn btn-outline-secondary btn-sm m-1" name="url" value="<%= monster.url %>">
              <%= monster.name %>
            </button>
            <% }) %>
          </form>
        </div>

        <% if (locals.stats) { %>
        <div class="card col-3" style="width: 18rem">
          <img
            src="https://www.dnd5eapi.co<%= stats.image %>"
            alt=""
            srcset=""
          />
          <div class="card-body">
            <h5 class="card-title"><%= stats.name %></h5>
            <p class="card-text">
              <%= stats.size %> <%= stats.type %>, <%= stats.alignment %>.
            </p>
          </div>
          <ul class="list-group list-group-flush">
            <li class="list-group-item">Hit points: <%= stats.hit_points %></li>
            <li class="list-group-item">XP: <%= stats.xp %></li>
            <li class="list-group-item">
              <span class="badge rounded-pill text-bg-primary">STR <%= stats.strength %></span>
              <span class="badge rounded-pill text-bg-primary">DEX <%= stats.dexterity %></span>
              <span class="badge rounded-pill text-bg-primary">CON <%= stats.constitution %></span>
              <span class="badge rounded-pill text-bg-primary">INT <%= stats.intelligence %></span>
              <span class="badge rounded-pill text-bg-primary">WIS <%= stats.wisdom %></span>
              <span class="badge rounded-pill text-bg-primary">CHA <%= stats.charisma %></span>
            </li>
          </ul>
          <div class="card-body">
            <a href="https://www.dnd5eapi.co<%= stats.url %>" class="card-link">Full stats</a>
          </div>
        </div>
        <% } %>
      </div>

      <footer>
        Made with 🌱 and 💗 in Copenhagen
      </footer>

    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm"
      crossorigin="anonymous">
    </script>
  </body>
</html>
